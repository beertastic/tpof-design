# Captain Jasu — files that exist on disk but must NOT reach Drive.
#
# Read by tools/publish-to-drive. One filename per line, `#` for comments.
#
# These four are the re-rolls in 11-production-tracking/Jasu-Image-TODO.md. Each
# failed for a reason that is now fixed in its slot prompt, and each is still in
# source/artwork because the replacement has not been generated yet. To rclone a
# failed plate is indistinguishable from a good one.
#
# Publishing them is the precise failure this tool exists to prevent: the costume
# department building from a superseded image. Delete a line the moment its
# replacement is generated and correct.
#
# Added 2026-08-04.

# RELEASED 2026-08-04 — captaining.png regenerated and correct. Took three
# passes: the generic dog was fixed by attaching the creature plates, then the
# akk came back half size twice until the size rule was promoted out of the
# prose. See Jasu-Image-TODO.md. The akk reads slightly under the belt landmark
# and that was accepted — this is a context image, and the animal's real spec
# is the measured 0.85 m in Creature.md, not this frame.

# RELEASED 2026-08-04 — headdress.png regenerated and accepted. The half-up
# knot is gone: hair fully down and loose, nothing gathered, nothing styled.
# Two things accepted with it, both deliberate. Its mantle came back
# DIAMOND-QUILTED, because this was the one close crop whose slot lacked the
# mantle-surface paragraph — mantle_detail is the authority on that surface,
# not this frame, and the paragraph is now in the slot for any re-roll. And
# its horns are a long sweeping crescent plus two shorter points, which obeys
# the asymmetry rule but does NOT match the five approved turnarounds. That
# contradiction is open and logged at the top of Jasu-Image-TODO.md.

# RELEASED 2026-08-04 — portrait.png regenerated and accepted. The mantle
# lattice is gone: the crazing is irregular and heavier at the shoulder fold,
# which is the slot text beating the attached photograph at close crop. The
# horns differ in shape — long sweeping point on her left, blunt stub on her
# right — but sit at matched height over each ear, so the arrangement still
# reads a little like a headset. Accepted for a casting and make-up plate.
# The fix, if it is ever wanted, is placement rather than more adjectives.

# Same two faults as portrait.png had, same fix.
expression_strip.png
